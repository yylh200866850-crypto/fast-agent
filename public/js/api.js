/**
 * API 调用封装
 */
const API = {
  conversations: '/api/conversations',
  config: '/api/config',
  providers: '/api/providers',
  skills: '/api/skills',
  mcp: '/api/mcp',
  tools: '/api/tools'
};

// 导出 API 端点
window.API = API;
